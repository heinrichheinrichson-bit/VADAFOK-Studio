# VADAFOK Studio 2.24.1 – Foundation – Card Creator Controller

Erster kontrollierter Schritt zur Aufteilung von `app.py`.

Ausgelagert werden:

- Laden der Recent-Template-Historie;
- Entfernen eines Recent-Eintrags;
- Validierung und Auswahl eines Templates;
- Orchestrierung der gezielten UI-Aktualisierungen nach der Auswahl.

Noch bewusst in `app.py`:

- Widget-Erstellung;
- Formularaufbau;
- Vorschau-Rendering;
- Export- und OBS-Aktionen.

Damit bleibt die Änderung klein und vollständig testbar.
