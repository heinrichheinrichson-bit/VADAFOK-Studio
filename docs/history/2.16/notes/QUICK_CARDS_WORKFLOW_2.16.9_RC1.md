# 2.16.9 RC1 – Quick Cards Workflow

Adds a compact workflow window for Quick Card favorites, recently used cards, and fast full-text search. The existing Quick Cards library and voice workflow remain unchanged. State is stored separately in `data/quick_cards_workflow.json`.
